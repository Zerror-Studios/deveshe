/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['emmpo.com','dieselfarm.com','plus.unsplash.com'],
  },
};

export default nextConfig;
